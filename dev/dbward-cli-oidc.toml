default_database = "app"
migrations_dir = "/workspace/db/migrations"

[server]
url = "http://dbward-server:3000"

[server.oidc]
issuer = "http://keycloak:8080/realms/dbward"
client_id = "dbward-cli"

[databases.app]
