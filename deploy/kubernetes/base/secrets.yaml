# Example secrets template — replace values before applying
apiVersion: v1
kind: Secret
metadata:
  name: dbward-secrets
  namespace: dbward
type: Opaque
stringData:
  agent-token: ""          # from: cat /data/agent-token (auto-created on first server start)
  database-url: ""         # postgres://user:pass@host:5432/db
